# --- 基础通信与 MCP ---
python-dotenv>=1.0.0
websockets>=12.0     # 修正：降低版本要求，兼容性更好
mcp>=1.0.0           # 建议放宽版本，避免过于严格的限制
mcp-proxy>=0.1.0
fastmcp>=0.1.0       # 建议放宽版本

# --- RAG 与 AI 核心组件 (必须添加) ---
langchain-core
langchain-openai     # 用于调用 DeepSeek/OpenAI 接口
langchain-chroma     # 用于向量检索
chromadb             # 向量数据库核心
pydantic>=2.0.0      # 基础数据验证
langchain-text-splitters
